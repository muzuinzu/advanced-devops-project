version: '3.8'

services:
  auth-service:
    build: ./auth-service
    ports:
      - "3001:3000"

  order-service:
    build: ./order-service
    ports:
      - "3002:3000"

  product-service:
    build: ./product-service
    ports:
      - "3003:3000"
